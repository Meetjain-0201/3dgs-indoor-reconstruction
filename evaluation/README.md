# evaluation/

Quantitative evaluation of reconstructed scenes.

Planned contents:

- `compute_metrics.py` — PSNR and SSIM between rendered novel views and held-out ground-truth frames.
- `split_train_test.py` — deterministic train / test split over the captured frames.
- `results/` — per-scene metric tables (gitignored when large).
